# Implementation Plans

Generated by the improve skill on 2026-06-16. Execute in the order below unless dependencies say otherwise. Each executor: read the assigned plan fully before starting, honor its STOP conditions, run its verification commands, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Report the package version from the CLI | P1 | S | - | DONE |
| 002 | Include scan diagnostics in reports and exit-code decisions | P1 | M | - | DONE |
| 003 | Prevent per-skill report filename collisions | P1 | S | - | DONE |
| 004 | Compare findings with stable, specific keys | P1 | S | - | DONE |
| 005 | Reject resource references that escape the skill directory | P1 | S | - | DONE |
| 006 | Populate elapsed scan duration in reports | P2 | S | - | DONE |
| 007 | Expose the grouped findings view in the review menu | P2 | S | - | DONE |
| 008 | Scope cross-ecosystem divergence checks by source | P2 | M | - | DONE |
| 009 | Add a companion agent skill for Skills Doctor | P2 | M | - | DONE |
| 010 | Add a public rule catalog for scanner findings | P2 | M | - | DONE |
| 011 | Define the package public API surface | P3 | M | - | DONE |
| 012 | Keep the review menu open after viewing findings | P1 | S | - | DONE |
| 013 | Ignore non-skill child directories during discovery | P1 | S | - | DONE |
| 014 | Classify asset resource findings explicitly | P2 | S | - | DONE |
| 015 | Allow custom roots when automatic discovery is incomplete | P3 | M | - | DONE |
| 016 | Hide repair subset choices that select no findings | P1 | S | - | DONE |
| 017 | Make the CLI module import-safe | P1 | M | - | DONE |
| 018 | Add line numbers to quality-rule findings | P2 | M | - | DONE |
| 019 | Preserve custom-root diagnostics in scan reports | P1 | S | - | DONE |
| 020 | Reflect diagnostic failures in score output | P1 | S | - | DONE |
| 021 | Report unreadable SKILL.md files | P1 | M | - | TODO |
| 022 | Fall back when handoff prompt writing fails | P2 | S | - | TODO |
| 023 | Migrate release publishing to npm trusted publishing | P2 | M | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale - finding fixed independently or approach abandoned)

## Dependency notes

- Plans 001-008 are independent bug/DX fixes and can run in parallel if executors avoid overlapping files.
- Plan 008 changes `SkillRecord`; coordinate with any executor touching quality-rule test helpers.
- Plan 009 should keep the CLI as source of truth and should not duplicate rule logic.
- Plan 010 can run before or after bug fixes, but if rule IDs change in other plans, refresh `docs/RULES.md` before finishing.
- Plan 011 is intentionally lower priority because it is a product/API decision, not a correctness bug.
- Plans 012-015 were added after plans 001-011 had already landed. They are fresh findings and should not re-open the completed work.
- Plan 012 is independent of plan 007: plan 007 exposed the grouped view; plan 012 keeps the menu alive after any view action.
- Plan 013 may affect the public `missing-skill` helper. Preserve compatibility unless a maintainer approves removing the exported helper.
- Plan 014 is a small report-schema/category polish. Coordinate it with any downstream consumer that treats `FindingCategory` as closed.
- Plan 015 should stay interactive-only unless a separate CLI flag design is approved.
- Plan 016 is independent and fixes an interactive repair edge case for warning-only or advice-only scans.
- Plan 017 is independent but touches the binary entrypoint; run the build gate before considering it done.
- Plan 018 is independent of plan 016 and plan 017. It should preserve rule IDs and finding messages so post-handoff comparisons stay stable.
- Plans 019-021 all touch diagnostics or scan health. They can be implemented independently, but if multiple executors run in parallel they must coordinate changes to `scanSkillRoots` and `test/reporting.test.ts`.
- Plan 020 becomes more valuable after plans 019 and 021 because those plans surface more diagnostic cases, but it does not strictly depend on them.
- Plan 022 is independent and isolated to repair handoff error handling.
- Plan 023 is a release-security migration. It requires external npm trusted-publishing configuration before token-based publishing can be fully retired.

## Findings considered and rejected

- Local agent `spawn` during repair handoff: accepted product behavior because the PRD requires launching a selected local agent after explicit confirmation.
- Writing `.skills-doctor/reports`: accepted product behavior for repair handoff artifacts.
- Missing full semantic trigger evals: accepted MVP non-goal; the PRD explicitly excludes expensive trigger/output eval suites from MVP.
- Re-reporting plans 001-011: rejected because `plans/README.md` marks them DONE and recent commits show the findings were implemented.
- Human summary does not print diagnostics: considered but not planned in this pass because plan 002 already established diagnostics in reports and exit-code decisions; this would be a follow-up UX polish, not a fresh high-leverage correctness fix.
- Re-opening missing-skill child-directory behavior: rejected because plan 013 is DONE and `test/domain-scan.test.ts` now encodes ignoring non-skill child directories.
- Re-opening custom-root discovery support: rejected because plan 015 is DONE and `test/cli-scan.test.ts` covers adding a custom skills path while standard roots exist.
- Missing CI: rejected because `.github/workflows/ci.yml` exists and runs `bun run verify` on pull requests and pushes to `main`.
- Local agent `claude`/`codex` approval-bypass flags: accepted product behavior because `docs/PRD.md` requires local repair-agent launch only after explicit confirmation.
- Re-opening missing full trigger/output eval execution: rejected because the PRD names expensive eval suites as an MVP non-goal, and the scanner already reports missing `evals/evals.json` for non-trivial skills.
